kind: ConfigMap 
apiVersion: v1 
metadata:
  name: {{ .Values.configmap.name }}
  namespace: {{ .Values.namespace }}
data:
  BG_COLOR: {{ .Values.configmap.data.BG_COLOR }}
  PAGE_TITLE: {{ .Values.configmap.data.PAGE_TITLE }}